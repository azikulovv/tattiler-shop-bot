# 🛍️ Tattiler Shop Bot

Телеграм-бот, созданный на **Node.js + TypeScript** с использованием
**[Telegraf v4](https://telegraf.js.org/)** и **[@edjopato/telegraf-i18n](https://github.com/EdJoPaTo/telegraf-i18n)** для мультиязычности.
Проект поддерживает строгую типизацию, алиасы `@/`, и легко масштабируется.

---

## 🚀 Технологии

- **TypeScript** — строгая типизация и надёжность
- **Telegraf** — современная библиотека для Telegram Bot API
- **@edjopato/telegraf-i18n** — поддержка многоязычности (i18n)
- **dotenv** — управление переменными окружения
- **zod** — валидация конфигурации
- **ts-node-dev** — горячая перезагрузка в dev-режиме
- **tsconfig-paths** — поддержка алиасов `@/`

---

## 📁 Структура проекта

```
tattiler-shop-bot/
├── src/
│   ├── bot/                # Логика Telegraf (сцены, команды, middleware)
│   ├── config/             # Конфигурации, работа с .env
│   ├── middlewares/        # Общие промежуточные слои
│   ├── locales/            # Файлы перевода (JSON или YAML)
│   ├── types/              # Типы (Context, Session, ...)
│   └── main.ts             # Точка входа приложения
├── dist/                   # Скомпилированные файлы (после build)
├── .env                    # Переменные окружения
├── package.json
└── tsconfig.json
```

---

## ⚙️ Установка и запуск

### 1. Клонировать репозиторий

```bash
git clone https://github.com/your-username/tattiler-shop-bot.git
cd tattiler-shop-bot
```

### 2. Установить зависимости

```bash
npm install
```

### 3. Создать файл `.env`

Пример содержимого:

```env
BOT_TOKEN=123456789:ABCDEF-your-telegram-token
```

### 4. Запуск бота

#### 🔧 Dev-режим (автоперезапуск)

```bash
npm run dev
```

#### 🏗️ Сборка

```bash
npm run build
```

#### 🚀 Продакшн

```bash
npm start
```

---

## 🌍 Мультиязычность

Модули локализации хранятся в `src/locales/`, например:

```
src/locales/
├── en.yaml
└── ru.yaml
```

Пример `en.yaml`:

```yaml
greeting: "Hello, {{name}}!"
```

Пример `ru.yaml`:

```yaml
greeting: "Привет, {{name}}!"
```

Использование:

```ts
ctx.i18n.t("greeting", { name: "Alex" });
```

---

## 🧩 Настройки TypeScript

- Алиасы `@/` определены в `tsconfig.json`:

  ```json
  {
    "baseUrl": "./src",
    "paths": {
      "@/*": ["*"]
    }
  }
  ```

- Поддержка ESM модулей: `"type": "module"`

---

## 🧠 Полезные команды

| Команда         | Описание                                               |
| --------------- | ------------------------------------------------------ |
| `npm run dev`   | Запуск с `ts-node-dev` (автоперезапуск при изменениях) |
| `npm run build` | Компиляция TypeScript → `dist/`                        |
| `npm start`     | Запуск скомпилированного кода                          |
| `npm install`   | Установка зависимостей                                 |

---

## 🧾 Лицензия

MIT © 2025 — Разработано с ❤️ и чистым кодом
